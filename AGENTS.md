# EDUKASS — постоянные правила для агентов

## Проект

Это статический сайт EDUKASS и математическая игра с реализованными главами «Üks ja kaks» и «Kolm».
Публичный домен: `edukass.ee`. Текущая видимая основа первой главы — принятая игра v28; версия 43 добавляет
утверждённую главу `Kolm` (миссии 16–33), сохраняя поведение миссий 1–15.
Версия 46 исправляет позиционирование карты по фактическим координатам текущей миссии и отключает scroll anchoring. Версия 48 отделяет математический прогресс от просмотра обновлённой сюжетной постановки. Версия 49 заменяет набор предметов непрерывным эпизодом путешествия: полёт → посадка → исследование → находка → возвращение → установка источника энергии → усиление ракеты → взлёт. Версия 51 закрепляет стандарт «богатство мира», исправляет физику горизонтального полёта и заменяет перемещение статичной кошки шарнирным циклом ходьбы с разворотом на обратном пути. Сюжетное действие проигрывается и при повторе миссии, но не начисляет награду повторно.

Перед работой прочитать:

1. `README_RU.md`;
2. `games/korrutamine-test/README_RU.md`;
3. `games/korrutamine-test/MISSION_CONFIG_RU.md`;
4. `docs/CREATIVE_RULES_RU.md`;
5. `docs/LANGUAGE_AND_SAVES_RU.md`;
6. актуальный паспорт проекта, если владелец приложил его к сессии.

## Границы файлов

- `games/korrutamine-test/chapter-one.config.js` — учебное содержание 33 миссий и границы двух глав.
- `games/korrutamine-test/locales/` — видимые языковые строки.
- `games/korrutamine-test/question-engine.js` — сборка и проверка заданий.
- `games/korrutamine-test/progress-store.js` — версии и миграции прогресса.
- `games/korrutamine-test/i18n.js` — языковой механизм.
- `games/korrutamine-test/game.js` — интерфейс, игровой цикл, анимация и звук.
- `games/korrutamine-test/game.css` — внешний вид и адаптивность.

Не возвращать учебное содержание миссий в `game.js` и не дублировать движок для
новой главы. Сначала расширить формат конфигурации и автоматические проверки.

## Неприкосновенные правила

- `CNAME` всегда содержит ровно `edukass.ee`.
- Видимый бренд — только EDUKASS; не возвращать KIISU MIISU и старый логотип.
- Интерфейс опубликованной игры остаётся эстонским.
- Внутри детской игры нет runtime-AI, сетевой генерации заданий или зависимости
  от внешнего сервиса.
- Миссия требует ровно 15 правильных ответов.
- Одинаковые примеры не показываются два раза подряд, включая возврат ошибки.
- Ошибочный пример возвращается позднее; сама ошибка не обнуляет миссию.
- Прогресс, статистика ошибок, звук и просмотр заставки сохраняются в браузере.
- Не менять существующие ключи хранения без миграции и прямого решения владельца.
- Сохранять шлем, уверенную эмоцию маскота и трёхфазный ритм награды v27–v28.
- В сюжетных главах награда — это прежде всего понятное действие и продвижение истории, а не новый декоративный предмет. Каждый полученный объект должен сразу иметь видимую функцию.
- Для всей игры действует стандарт «богатство мира»: сцена должна быть интересной в стоп-кадре и содержать главное действие, 2–3 средних ориентира, 4–8 мелких уместных деталей и хотя бы одну мягко движущуюся деталь. Пустой одноцветный фон не считается готовой средой.
- Персонаж не перемещается назад лицом вперёд и не имитирует ходьбу прыжками всей статичной картинки. Для ходьбы используются кадры или шарнирная анимация лап; при движении в обратную сторону персонаж разворачивается.
- Повторное прохождение может повторять короткую сюжетную сцену для удовольствия, но не дублирует постоянный предмет, энергию или прогресс.
- Не менять механику, тексты, таймеры, графику или звук «заодно» с архитектурной
  задачей.
- Не удалять пользовательские или исторические файлы без отдельного указания.
- Не публиковать сайт и не менять DNS без прямого указания владельца.

## Обязательные проверки

Из корня проекта выполнить:

```bash
node scripts/audit-project.js
```

Команда включает синтаксис JavaScript, локальные ссылки, конфигурацию 33 миссий,
99 000 сгенерированных раундов, защиту неизменности миссий 1–15, языковую основу, миграцию старого сохранения,
бренд, `CNAME` и порядок загрузки файлов.

Если менялся интерфейс, дополнительно проверить игру визуально минимум при
размерах 390×844, 844×390 и 1440×900. Если браузерная среда недоступна, честно
зафиксировать это как непроверенный пункт.

## Цикл изменения и независимой приёмки

1. Зафиксировать задачу и список того, что нельзя сломать.
2. До правок сохранить контрольные результаты проверок.
3. Исполнитель меняет только согласованный слой.
4. Выполнить `node scripts/audit-project.js`.
5. Независимый проверяющий получает исходную задачу, эти правила и готовую
   сборку, но не редактирует код и не опирается только на отчёт исполнителя.
6. Проверяющий выдаёт факты по приоритетам P0–P3 и отдельно отмечает ограничения
   проверки.
7. Владелец визуально принимает пользовательские изменения.
8. Только после приёмки обновить историю версий и паспорт.

Подробный протокол: `docs/INDEPENDENT_AUDIT_CYCLE_RU.md`.
